package tools.vitruv.vitruvAdapter.core.impl.selector

class AllSelectorTest {
    @org.junit.jupiter.api.Test
    fun testApplySelection() {
//        val viewSelector = Mockito.spy(ViewSelector::class.java)
//        val eObjects = EObjectContainer.getContainer1()
//        Mockito.doReturn(eObjects).`when`(viewSelector).selectableElements
//        val elementViewSelection = ElementViewSelection(eObjects)
//        Whitebox.setInternalState(underTest, "person", mockedPerson);
//        val selectAbleElements = viewSelector.selectableElements
//        val allSelector = AllSelector()
//        allSelector.applySelection(viewSelector)
//        for (eObject in eObjects) {
//            assertEquals(true, viewSelector.isSelected(eObject))
//        }
    }
}
